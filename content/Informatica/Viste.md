Tabelle virtuali create a partire dalle tabelle. Servono per livello esterno.

Possono limitare la visibilità delle tabelle del DB, garantendo sicurezza e semplicità

```sql
CREATE VIEW Vista AS
SELECT ...
FROM ...
WHERE ...
```